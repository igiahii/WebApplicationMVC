@{
    ViewData["Title"] = "Home";
}

<div class="text-center p-3 d-flex flex-column justify-content-center" style="height:80vh">
   <div >
        <h1 class="display-3 fw-bold">Welcome to category list</h1>
        <p class="fs-3">
            <i class="bi bi-arrow-right-circle-fill"></i>&ensp;Go to the <a asp-controller="Category" asp-action="Index">Category Tab</a> for more information
        </p>
   </div>
</div>
